Done - todas las cifras de Importes han de representarse con 1 decimal y han de dividirse por 1000
Done - haz que las columnas de la lista sean ajustables. 
Done - elimina el boton de accion, haz que el campo Expediente sea clickable para navegar a la pantalla de detalle
Done - añade un parametro "calcular idoneidad en Import" para decidir si durante el import hay que calcular idoneidad (para evitar sobrecargar el modelo). Si el parametro esta en NO entonces el campo de idenoidad no se calcula durante el import. Si el campo no se calcula entonces el valor sera "no calculado".   
Done - el campo ideneidad en la pagina de lista sera clicable cuando tenga valor "no calculado"; si se pulsa se invoca la funcion de descripta en el Caso de Uso 01